<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Misskey_SMTC.Pages.EditTemplate.EditTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Misskey_SMTC.Pages.EditTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="stackDivider" TargetType="Rectangle">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Fill" Value="{StaticResource DividerStrokeColorDefaultBrush}"/>
            <Setter Property="Margin" Value="12 8"/>
        </Style>

        <Style x:Name="templateContentStack" TargetType="StackPanel">
            <Setter Property="Padding" Value="16 0"/>
            <Setter Property="Spacing" Value="4"/>
        </Style>
        
        <Style x:Key="templateHeaderText" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
            <Setter Property="IsTextSelectionEnabled" Value="True"/>
        </Style>
        
        <Style x:Key="templateExampleText" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
            <Setter Property="Foreground" Value="{StaticResource TextFillColorSecondaryBrush}"/>
        </Style>
    </Page.Resources>

    <Grid ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <TextBlock Text="テンプレートの編集" Style="{StaticResource TitleTextBlockStyle}" Grid.Column="0" Grid.Row="0" Margin="0 0 0 24"/>
        <StackPanel Grid.Column="0" Grid.Row="1" Spacing="12">
            <TextBlock Text="テンプレート" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <TextBox Text="{x:Bind TemplateText, Mode=TwoWay}" TextWrapping="Wrap" AcceptsReturn="True" PlaceholderText="#NowPlaying: {title} - {artist} / {album}\n{url}" FontFamily="Consolas" MinHeight="120"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1" Spacing="12">
            <TextBlock Text="テンプレート文字列" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <ScrollView>
                <StackPanel Background="{StaticResource LayerFillColorDefaultBrush}" Padding="0 8">
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{title}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.Title}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{subtitle}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.Subtitle}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{artist}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.DisplayArtist}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{album}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.DisplayAlbum}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{albumartist}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.AlbumArtist}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{tracknumber}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.TrackNumber}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{albumtrackcount}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.AlbumTrackCount}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{genres}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{x:Bind SongInfo.GenresText}" Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                    <Rectangle Style="{StaticResource stackDivider}"/>
                    <StackPanel Style="{StaticResource templateContentStack}">
                        <TextBlock Text="{}{url}" Style="{StaticResource templateHeaderText}"/>
                        <TextBlock Text="{}https://music.apple.com/..." Style="{StaticResource templateExampleText}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollView>
        </StackPanel>
    </Grid>
</Page>
